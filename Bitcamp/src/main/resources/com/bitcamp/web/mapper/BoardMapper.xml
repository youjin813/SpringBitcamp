<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bitcamp.web.mapperImpl.BoardMapperImpl">
	<select id="selectAll" resultType="board" parameterType="cmd">
	SELECT t.* FROM
	(SELECT ROWNUM r, t.*
	FROM(SELECT 
    bbs_seq AS bbsSeq,
    title,
    content,
    regdate,
    id 
    FROM Board
    order by bbs_seq desc)t)t
    where r between #{paging.startRow} and #{paging.endRow}
    order by t.r asc
	</select>
	<select id="selectCount" resultType="int">
		SELECT COUNT(*) FROM Board
	</select>
	<select id="selectBySeq" resultType="board" parameterType="cmd">
		SELECT bbs_seq AS bbsSeq,title,content,regdate,id FROM Board WHERE bbs_seq LIKE #{board.bbsSeq}
	</select>

</mapper>